[project]
name = "marine-boids"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "kamouaferid@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
marine-boids = "marine_boids.main:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
